{
  "dependencies": {
    "mgplus": "^1.2.5",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "scripts": {
    "build": "esbuild ./frontend/src/index.jsx --bundle --outdir=./frontend/out",
    "start": "npm install && python3.11 -m venv ./.venv && source ./.venv/bin/activate && pip install -r requirements.txt && mkdir -p ./storage/app && npm run build && python backend/main.py"
  },
  "parserOptions": {
    "ecmaVersion": 6,
    "sourceType": "module"
  },
  "devDependencies": {
    "esbuild": "0.20.2"
  }
}
